schema: http://localhost:4000/graphql
overwrite: true
watch: true
generates:
  src/generated/types.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-mongodb"
 
